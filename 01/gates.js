(function generateGates(name, size) {
  for (i = 0; i !== size; i++) {
    // console.log(`${name}(a=a[${i}], b=b[${i}], sel=sel, out=out[${i}]);`);
    // console.log(`${name}(a=a[${i}], b=b[${i}], out=out[${i}]);`);
  }
})("Mux", 16);
